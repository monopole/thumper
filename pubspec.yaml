name: thumper
description: Widget for controlling an Iterable. Shows reset, forward step, play, pause and speed controls.
version: 0.1.5
homepage: https://github.com/monopole/thumper

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  test: ^1.14.2
  bloc: ^3.0.0
  flutter_bloc: ^3.2.0
  meta: ^1.1.8
  tuple: ^1.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
# pedantic: ^1.0.0

flutter:
  uses-material-design: true
